[package]
name = "renamer-rust"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
chrono = "0.4.44"
clap = { version = "4.6.0", features = ["derive"] }
regex = "1.12.3"
